version: '3.8'

services:
  mongodb:
    image: mongo:4.4
    container_name: answer-service-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: answer-service
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
